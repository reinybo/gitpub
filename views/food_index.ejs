<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <metsa name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
    </head>
    <body>
        <h1>Foods</h1>
        <ul>
            <% for(let i=0; i < allFoods.length; i++) { %>
                <% let capFood = allFoods[i].name[0].toUpperCase() %> 
                <li>
                    <a href="/foods/<%=i%>"><%= capFood + allFoods[i].name.slice(1)%></a>
                </li> 
            <% } %> 
        </ul>
        <h3><a href="/drinks">See drinks</a></h3>

    </body>
</html>

<style>
    body {
        color: aqua;
        background-color: pink;
    }
</style>